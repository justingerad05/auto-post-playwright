name: Medium Auto Post

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Install Node dependencies
      - name: Install dependencies
        run: npm install

      # 3️⃣ Install Playwright browsers
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      # 4️⃣ Run login script
      - name: Load Medium Profile
        run: npm run login
        env:
          MEDIUM_COOKIES: ${{ secrets.MEDIUM_COOKIES }}

      # 5️⃣ Run test post script
      - name: Send Test Post
        run: npm run test-post
        env:
          MEDIUM_COOKIES: ${{ secrets.MEDIUM_COOKIES }}

      # 6️⃣ Run actual post script (optional)
      # - name: Post Article
      #   run: npm run post-article
      #   env:
      #     MEDIUM_COOKIES: ${{ secrets.MEDIUM_COOKIES }}
